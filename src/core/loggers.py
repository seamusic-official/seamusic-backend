from logging import getLogger


api = getLogger('api')
services = getLogger('api')
core = getLogger('core')
repositories = getLogger('repositories')
converters = getLogger('converters')
utils = getLogger('utils')
